import {mkdirSync,readFileSync,writeFileSync} from 'node:fs';
const out='test-results/flight-delivery';mkdirSync(out,{recursive:true});
const before=JSON.parse(readFileSync('test-results/astra-desktop-flight-before/report.json'));
const after=JSON.parse(readFileSync('test-results/astra-desktop-flight-after/report.json'));
const fixed=n=>Number.isFinite(n)?n.toFixed(1):'—';
const rows=after.performance.filter(p=>p.p95Ms).map(p=>{const b=before.performance.find(q=>q.name===p.name)||{};return `<tr><td>${p.name}</td><td>${fixed(b.p95Ms)} → ${fixed(p.p95Ms)} ms</td><td>${fixed(b.p99Ms)} → ${fixed(p.p99Ms)} ms</td><td>${fixed(b.retainedHeapMiB)} → ${fixed(p.retainedHeapMiB)} MiB</td></tr>`;}).join('');
const pairs=[['Boss frame removed','boss-frame'],['Authored wave entrances','wave'],['Wave-clear presentation','reward']].map(([name,key])=>`<h2>${name}</h2><div class="pair">${['before','after'].map(side=>`<figure><a href="../flight-revision/${key}-${side}.png"><img src="../flight-revision/${key}-${side}.png" alt="${name}, ${side}"></a><figcaption>${side}</figcaption></figure>`).join('')}</div>`).join('');
writeFileSync(`${out}/index.html`,`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Nova Swarm · flight revision</title><style>body{margin:0;background:#080e16;color:#dbe9f1;font:16px/1.6 system-ui}main{max-width:1400px;margin:auto;padding:40px}h1{font-size:42px}h2{margin-top:40px}p{max-width:900px;color:#a9bdcd}.pair{display:grid;grid-template-columns:1fr 1fr;gap:16px}figure{margin:0}img,video{width:100%;background:#000;border:1px solid #294656}figcaption{color:#80d8df;text-transform:capitalize}a{color:#80d8df}table{border-collapse:collapse;width:100%}td,th{padding:12px;text-align:left;border-bottom:1px solid #294656}@media(max-width:800px){.pair{grid-template-columns:1fr}main{padding:18px}}</style><main><h1>Nova Swarm · flight revision</h1><p>Six normal-wave routes, ten boss-family patrols, occasional later-phase feints and stronger wave-clear presentation. The unwanted pink frame is removed from every boss.</p><p>Actual running game captures, with matched resolution within each pair. These are staged QA scenes, not offline renders or evidence of human playtesting.</p>${pairs}<h2>Actual Windows build</h2><div class="pair"><figure><video controls preload="metadata" src="../astra-desktop-flight-after/gameplay-normal-speed.mp4"></video><figcaption>Dense combat · scripted keyboard input</figcaption></figure><figure><video controls preload="metadata" src="../astra-desktop-flight-after/boss-destruction-normal-speed.mp4"></video><figcaption>Boss encounter and staged defeat</figcaption></figure></div><p>Normal wall-clock speed, silent recordings. Isolated profiles with QA invulnerability; boss defeat is triggered through the damage API.</p><h2>Performance · before → after</h2><table><tr><th>Scene</th><th>95th percentile</th><th>99th percentile</th><th>Retained JS heap</th></tr>${rows}</table><p>Same PC, 1280×720 and QA procedure; Daily uses the same daily seed. Dense combat is comparable, not an identical input replay. Recording is outside timing measurements. Existing background applications were left alone; this is not a controlled laboratory benchmark or a zero-stutter guarantee.</p><h2>Difficulty and rollback</h2><p>Sectors 1–3 retain entry timing. Eligible entrances from sector 4 are 6% shorter. Health, damage, attack budgets and locked warnings remain. Changed positioning still needs human difficulty comparison. Use <b>--nova-flight-previous</b> for the prior entrances, patrols and wave-clear treatment; the unwanted frame stays removed.</p><p><a href="../../docs/astra-flight-delivery-20260907.md">Build, Steam receipt, tests and limitations</a></p></main></html>`);
console.log(`${out}/index.html`);

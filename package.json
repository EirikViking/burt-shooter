{
  "name": "burt-game",
  "version": "1.0.0",
  "description": "Kurt Edgar og Eirik sitt arkade shooter spill",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-ranks.cjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "pixi.js": "^8.15.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "vite": "^5.0.11",
    "wrangler": "^4.59.1"
  }
}
# Core and serpent expansion

Baseline: `67c84381fb3e3304fa99a2424b9583905a3dafa4`, checked-out branch `codex/astra-visual-overhaul`. Root instructions, handoff and prior balance report read; fetch, status, branch, HEAD and unique worktree ownership verified. Existing opening balance and precision-hull improvements retained. Initial premature implementation was disclosed and paused; the user then explicitly approved the revised core rewards and full update.

## Gameplay and presentation

- Ten bonus cores grant collection rewards, never combat powers: Treasure, Pursuit, Daredevil, Survivor, Hunter, Collector, Constellation, Jackpot, Archive and Relic. Their art, colors, movement, labels and ElevenLabs sounds are distinct. On-screen values reflect the current score calculation. Codex tips describe base rewards and explain score scaling.
- A shared randomized schedule spaces cores by 2–4 sectors, averaging three. It selects a wave and arrival delay, rather than rolling every frame. Missing a core breaks the Collector chain. Core drops from snakes spend the same budget. Archive recovers previously unknown Cabinet logs; three Relics collected across runs unlock automatic gold inlays clipped to the ship's hull. Persistent progress uses existing threat-discovery metadata and session rules, with no new save or Cloud key.
- Four Space Snake species first become eligible at sector 6. Ordinary eligible waves roll a 20% encounter chance with a separate species roll; there is no fixed every-fifth-wave schedule. Boss/challenge/reinforcement/authored special/Daily/experimental waves retain their rules. Real connected sections can be destroyed independently; the surviving front keeps hunting. All sections must die for the final bounty and celebration. Four complete translated Codex backstories and tips accompany distinct, related ElevenLabs monster screams and death cries.
- Six rotating enemy rig families use generated machined armor, counter-rotating tilted layers, energy tracks and satellites. Decorative geometry does not consume gameplay randomness. Reduced-motion behavior is retained.
- Boss fuel deliveries heal 50% more in sectors 1–20. Existing support-group and single-support modifiers remain; sector 21 onward retains its prior fuel values.
- Current sector is the prominent headline in the existing central mission frame. The rejected large gold sidebar box is removed. Narrow displays place the mission frame below the top HUD to prevent frame overlap. The passive trait card retires after an eight-second introduction; actionable experimental recharge/ready notices remain. The pause screen shows the full trait description. Existing ship-centered phase cooldown/ready cues remain.

## Art and sound provenance

Final sprites are under `public/art/core-serpent/`: ten core sprites, four monster heads, one shared articulated body sprite and a machined orbit ring. Built-in OpenAI image generation produced the final transparent sprites. `docs/core-serpent-models-20260907/imagegen-subjects.json` records the visual subjects and workflow. Editable Blender source models and their superseded draft renders remain under that same docs directory; they are not the final in-game sprites. The original Blender recipe is `scripts/render-core-serpent-assets.py`.

All new audio is ElevenLabs output from a verified active commercial plan. Requests, original hashes and preserved MP3s are under `docs/core-serpent-audio/`. Final audio has 3 dB of gain reduction for headroom, recorded in `mastering.json`; no local synthesis was used. Twenty-one files decode successfully at 44.1 kHz with valid durations and signal levels. These checks do not claim a human audition.

## Changed files and localization

The main gameplay files are `src/config/BonusCoreCatalog.js`, `BonusCoreCadence.js`, `SpaceSnakes.js`, `BossSupportShips.js`, `src/progression/BonusCoreRewards.js`, `src/entities/BonusDrone.js`, `SpaceSnake.js`, `Player.js`, `src/managers/EnemyManager.js` and `src/scenes/PlayScene.js`. Presentation changes include `src/effects/EnemyOrbitRig.js`, `RelicHullDetail.js`, `SpaceSnakeDeath.js`, `src/ui/HUD.js`, `src/scenes/ThreatCodexScene.js` and `src/utils/GameAssets.js`, plus their Codex/audio catalogs and localized dictionaries under `src/i18n/`. New art/audio and their generation receipts are listed above; corresponding focused checks and compact-Codex QA are under `scripts/`.

All eight supported interface languages include the new content and pause-trait explanations. No known new untranslated interface text remains; intentional proper names and existing English audio are preserved. Automated coverage is not human translation approval, and no Steam language-support setting was changed.

## Validation to date

- Reward/cadence tests: ten independent rewards, no combat-power grants, bounded payouts, random 2–4-sector gaps averaging three, no snakes before six, randomized snake sample 1,934/10,000 with 31 observed gap lengths, path bounds at three resolutions and fuel boundaries through sector 410.
- Threat Codex: 14 categories, 2,482 entries; new core/snake text present in all eight supported languages. Source i18n, release-line, 396-key audio catalog and prebuild checks passed.
- Staged runtime: all ten actual reward grants leave combat powers unchanged, duplicate collection does not pay twice, all four snakes remain connected and damageable, final bounty/core obeys cleanup rules, Relic cosmetic unlocks, six textured orbital families. Sixteen Codex screenshots cover both new categories in eight languages. Corrected an initial QA language setter that addressed a duplicate source module; the final test asserts the running game's actual locale. Uppercase category lookup aliases were added after visual review found English tab labels.
- Actual keyboard input: a real player collision collected Treasure. A staged level-six Cinder Maw was defeated in 16.839 seconds without invulnerability or forced damage, ending with two lives. Bounty/core and pause trait explanation verified. This is staged scripted QA, not natural progression to sector six or evidence of human enjoyment.
- Existing HUD readability check passes. Its assertion now permits a deliberately hidden trait card while retaining priority checks whenever the card is visible. The legacy trait introduction remains available. An earlier staged snake-input test accidentally scheduled ordinary wave cleanup before the snake; that fixture was corrected and its failure log retained. A separate expanded runtime pass was interrupted by source HMR; final stable passes supersede it without deleting evidence.

### Final review corrections

Runtime checkpoints: `f6e35dc` adds the expansion, `c1c058c` translates every ship-trait summary, and `beabbf2` keeps the Codex readable at the minimum window size. Visual review found that the original automated language snapshot did not include the new pause explanation; it now does, and all 29 runtime summaries have explicit coverage in seven non-English languages. The corrected production build passed its 80-screen language check. A later compact-window review found overlapping Codex story/tip sections at 960×600. The complete dossier now scales to fit instead of compressing its sections. Seventeen captures cover both new categories in all eight languages at 960×540 and a further 960×600 view; story bounds, mouse-wheel scrolling and scrollbar dragging pass.

The production feature check confirms actual player/core collision, all ten rewards without power changes, shared rarity enforcement, four connected and damageable snakes, and exactly one recorded defeat per species in an isolated save. The earlier zero-defeat Codex captures were explicitly unranked QA, where the inherited defeat-recording policy intentionally does not persist kills. The later isolated ranked-policy check verifies the real persistence path without connecting to Steam. The final installed web-game skill client also passed with an inspected gameplay capture.

Superseded package jobs were stopped after the translation and compact-Codex corrections. Their outputs and failures remain separate. A source-language retry timed out opening the leaderboard; another compact test lost its screenshot connection during memory pressure, and a restart attempt ran before the local server was ready. These are retained, with successful stable checks identified separately. Browser jobs are now sequential. The earlier production smoke passed with zero page errors/bad responses and five Pixi warning stack messages. A concurrent later smoke timed out in gamepad navigation and required the later sequential retry recorded below.

The final build reuses the unchanged `c1c058c` public-asset snapshot. All 8,587 snapshot/output pairs are verified as the same physical files using filesystem IDs, with the public Git tree unchanged and source/snapshot sizes equal. The build writes only new generated JavaScript/CSS/index files into its own output; source assets and prior generated bundles remain unchanged. The evidence is `test-results/core-serpent-final-assets-proof.json` and the temporary Vite config under `test-results/`.

The `beabbf2` staged Windows package passed the full new-feature check and all twelve desktop steps. The strict native controller check passed every keyboard/gamepad action but failed on one Pixi warning: the orbit armor sprite was attached directly to a Graphics object. Commit `d6b16a2` gives each ring a proper Container holding its armor and energy tracks. Follow-up review found the same deprecated attachment in the Relic hull decoration; `8b2b987` places it beside the ship, copies the ship transform after tilt updates, and refreshes the mask when the hull texture changes. The failed strict check is retained. The twelve-step samples on the prior package had p95 frame times of 16.9 ms (Daily), 17.1 ms (sector 90) and 16.9 ms (boss); these are short staged samples, not a fix for the inherited isolated stall.

The corrected `8b2b987` production feature pass has zero browser errors or warnings, all ten rewards and four snakes, and exact Relic rotation/scale agreement with the actual hull mask. The general production smoke now passes every existing gameplay assertion, including the startup and gamepad stages that timed out in earlier concurrent runs. Its retained console record contains one `net::ERR_NO_BUFFER_SPACE` resource message; there were no page exceptions or HTTP failures. Its cause is not established. The installed web-game client also completed on this runtime. Final native checks run on the separately staged package and retain stricter console gates.

The corrected Windows executable is `test-results/astra-build-2026-09-07T19-19-26-670Z/win-unpacked/Nova Swarm.exe`, containing runtime `8b2b9870c018c0a9033b045eb55283b80f75667f`. The standard packager completed successfully; Steam runtime staging and verification passed. Its native feature pass has zero warnings/errors, actual collection, ten independent rewards, all four Codex defeat records, shared cadence and Relic alignment. Five-second snake samples each rendered 300–301 frames, p95 16.8–17.1 ms, maximum 17.5 ms. Native startup and every keyboard/gamepad movement, firing and pause check pass with no console events. All twelve final desktop steps pass. Their p95 frame times were 16.8 ms (Daily), 17.1 ms (sector 90) and 17.1 ms (boss), with a largest sampled boss frame of 33.8 ms. The isolated one-minute performance gate passes at 59.86 FPS average and 58.82 FPS minimum sampled reading, with no console events or errors.

Final evidence is under `test-results/core-serpent-native-features-delivery/`, `astra-desktop-core-serpent-delivery/`, and `astra-native-core-serpent-delivery-{smoke,control-smoke,perf-smoke}/`. The upload preparation gates all these reports, verifies the packaged runtime identity, and hashes every copied file. All 410 files, totaling 1,686,387,797 bytes, match the tested package. Packaged/upload ASAR SHA256: `f0ce54f169e4e080743b85804a5a336c48d8f5f67452aa13cdb1fa440ee9fe61`.

Steam upload completed and was verified on the server at 19:42 UTC on 7 September 2026: **Build 25173832**, branch **`sector-continue-test`**, AppID **4765070**, depot **4765071**, manifest **6100946787939831172**. The receipt is `test-results/core-serpent-steam-upload-20260907-8b2b987/receipt.json`; this directory also contains the exact VDF, full file-hash proof, Steam build logs and before/after server records. Public/default remains **25169120**, `test-build` remains **23782673**, and the server Cloud configuration block is identical. Steamworks service settings, public announcements and store media remain untouched. No public/default deployment was performed.

## Retained limitations and rollback

The three inherited checks recorded in the handoff remain outside this change: two expect five opening waves despite the accepted three-wave opening, and one expects an absent Cabinet Wonder clock guard. The prior isolated 13.1-second sector-90 stall remains unexplained; this pass does not claim to fix it. The user's testing bot has not been supplied or used. Local QA uses isolated profiles and offline/mock platform services, with no real save, achievement, Cloud or leaderboard submission.

All implementation edits are task-owned and checkpointed locally. Source rollback, after a clean ownership/status check: `git revert 8b2b987 d6b16a2 beabbf2 c1c058c f6e35dc`. Steam rollback is separate: assign previously verified Build **25169120** to `sector-continue-test` only; reverting Git alone does not change Steam. A redundant fetch during resume did not return promptly and was stopped; branch/HEAD/status and unique worktree ownership were rechecked, with no pull, merge, rebase or baseline switch. A later helper's inherited Git filesystem-monitor call also stalled; only the owned helper was stopped, then the same read-only checks completed with that monitor disabled and a timeout.

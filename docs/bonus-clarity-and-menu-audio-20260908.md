# Bonus drone art, exact rewards and menu audio choice

Baseline: `72c309d85a9563c05e1af3b65eaf4a64bbeba82f`, branch `codex/astra-visual-overhaul`, uniquely owned by `D:\vibe-coding-e\nova-swarm-forum-129-improvements-20260822`. Fetch, clean status, branch, log and worktree ownership were verified before implementation. Existing fleet, snakes, menus, ElevenLabs audio and earlier decisions are preserved.

All 19 shootable bonus drones now have individual ImageGen artwork, with distinct hulls, cargo structures, colors and industrial materials. Transparent 1254px originals produce lossless 1024px Codex previews and 256px gameplay sprites. Stable discovery IDs and texture indices are preserved; unrelated legacy drone texture slots 4–11 are unchanged. Prompts/source hashes and the 38 runtime asset hashes are recorded in `bonus-drones-20260908/`. The original generated files remain at their recorded local paths. A white clipping rectangle was also visible while Codex artwork loaded; clipping geometry now stays non-renderable until used as a mask.

The advertised raw reward previously passed through combat normalization (0.1 × 1.265), global tuning, combo and other multipliers. This explains why 1000 could award about 145. Bonus drone/core rewards now bypass those combat multipliers and award the literal advertised number. Ordinary enemy scoring is unchanged. Variable cores keep their existing conditions and update their visible value every frame. Score totals, bonus breakdown, rank updates and final-score lock still use the existing scoring path. The exact-bounty decision intentionally makes these prizes more valuable; preserving old leaderboard positions was not a priority. Drone Codex explanations are updated in all eight languages.

Ambient shootable drone intervals are divided by 0.65, preserving randomized spacing and reducing average eligible-time arrivals by 35%. The separate rare random bonus challenge chance is also multiplied by 0.65. Scripted challenge waves and collectible core cadence are unchanged.

Settings → Audio includes Menu Audio: Ambience or Music, with ambience as the default. Existing ElevenLabs recordings supply both choices. Choice changes crossfade, persist locally and pass through the existing Cloud save sanitizer/restore schema. Menu navigation keeps the chosen audio; gameplay still takes over on launch. Menu voices remain off by default and retain their separate setting. No new sound synthesis or audio generation was used.

Validation: release-line, i18n, Steam bridge and local Cloud save tests pass. Eight-language UI check produced 80 captures with no errors, untranslated leaks or placeholders. Eight additional 720p Audio layouts were checked, plus Warden and Longwave in the actual Codex. Real settings click/reload persistence, ambience/music switching, rapid reversal, Hangar return, all ten core reward formulas, exact bounty ledger increments, ordinary score behavior and final lock passed. Actual collision tests with elevated multipliers awarded 850 for an 850-point drone and 600 for a 600-point core. Controller-only flow passed against the current source server. Early screenshot fixtures were corrected to use the normal discovery/refresh path; white images during cold asset loading revealed the clipping-mask fix above.

The complete static candidate contains 10,461 files: 10,423 unchanged files linked from the prior immutable complete build only after exact byte/hash comparison, and 38 new images copied from source. No mutable source asset is hardlinked. Candidate: `test-results/bonus-clarity-dist`; proof: `test-results/bonus-clarity/static-proof.json`.

Production packaging and Steam test delivery are pending and will be recorded below. Target remains `sector-continue-test`; prior verified test build is `25192515`. No public release, Steamworks configuration, forum draft or public post is changed. No full campaign, human balance verdict, live leaderboard submission or Steam Cloud round trip is claimed. Historical sector-90/performance and pacing observations remain unresolved; the user's testing bot is still unavailable.

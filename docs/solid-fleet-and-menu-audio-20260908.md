# Solid player fleet and continuous menu ambience

Baseline: `c9413224e94b45a728daa73e016b2e161b8c018f` on `codex/astra-visual-overhaul`, uniquely owned by `D:\vibe-coding-e\nova-swarm-forum-129-improvements-20260822`. Fetch, branch, log, clean status and worktree ownership were verified before changes. The checked-out version is the baseline. Earlier drones, snakes, core rewards, menu buttons and community decisions are preserved.

The user rejected the relief/height-map fleet. Thirty original Blender-authored solid meshes replace that approach. Each has an individually specified architecture, closed volumetric fuselage, modeled engines and underside, separated armor, deliberate livery, weapons, machinery and emissive materials. Original ship art informs colors and personality; exact silhouette copying was not required. Stats, traits, collision radii and unlock requirements are unchanged. GLBs contain no raster hull textures. The same models supply gameplay sprites and lossless showroom portraits. Reproduction: Blender background with `scripts/build-solid-fleet.py -- player 30 0`, then `node scripts/finalize-solid-fleet.mjs`. Editable masters and hash manifest are in `docs/solid-fleet-20260908/`.

`AstraTurntable` now uses live Three.js GLBs through the existing Pixi scene graph. A single shared WebGL renderer serves the selected ship; models and shadow resources are released when selection changes or menus close. Normal close-up resolution is 1536 square, increasing for large windows up to 2560; Hangar cards use 768. Static views do not render again until their angle changes. Horizontal and vertical drag inspect actual geometry; there is no low-resolution atlas swap while dragging. Matching rendered portraits provide a fallback if WebGL/model loading fails. No external generation service or new audio generation was needed.

Menu audio is owned by scene routing. Main menu, Hangar, ship details, achievements, leaderboard and Codex share the existing four ElevenLabs ambience stems without stopping/restarting them. The old Hangar call that started gameplay music was removed. Gameplay music starts only after a successful transition to Play; rejected launches cannot leave normal songs running in a menu. A stale fade callback cannot pause a newer gameplay context, and queued song starts cannot enter an ambient context.

Menu voices default off. The localized Audio toggle is present in all eight languages, gates even force/ignore-global-voice requests in menus, stops active/delayed menu voices immediately when disabled, and persists through the existing local/Cloud save schema. Global voice and boss voice preferences remain separate. The existing ElevenLabs ambience recordings and game sounds are unchanged; no synthesized replacement sounds were added.

Validation so far: release-line; i18n; 80 UI captures across all eight languages with no page errors or untranslated leaks; eight additional 720p Audio layouts; direct menu/Hangar/details/return continuity, voice default/opt-in/mute, gameplay fade and music mute recovery; global voice-mute contract; local save sanitization and restore including both new preference values. All 30 models rendered in the actual viewer from above and below with one resident model. Final production/native checks and Steam delivery will be recorded below. Early preview attempts missed new files because the source server had cached its public-asset inventory; after a fresh server start the complete-asset UI run passed. Those failed attempts are not counted as passes.

Limits: no full campaign, human fun/balance verdict, live leaderboard submission or Steam Cloud round trip. The historical sector-90 stall and pacing/Overrun observations remain unresolved. The user's testing bot is still not supplied. No public posts, public release, Steamworks configuration or marketing changes are authorized by this batch. Target test branch remains `sector-continue-test`; previous verified test build is `25189882`.

Final runtime: `9a13182`, following implementation commit `91fece2`. All 30 models were regenerated with a separate machined ventral keel and structural mounts to remove intersecting underside shells. A final audio guard prevents stale combat callbacks reclaiming music while in menus. The Three.js MIT license is included in the shipped public assets.

After the interrupted session, exports 26–30 were rebuilt completely. Final asset manifest validation and all 30 live top/underside viewer checks passed with no page errors and zero retained models after disposal. The audio check also passed the stale-victory-context guard. Its fixed 2.6-second playback assertion was replaced by a bounded wait for actual playback because disk activity delayed loading; the runtime was unchanged for that fixture correction. Steam bridge and local Cloud-save preference checks passed again.

The final production guard chain and `build:current` passed. The complete static payload was assembled from current public files: 10,272 files linked from a previous immutable complete build only after exact source/hash comparison, plus the new license copied from source. All 150 final fleet files were copied separately. No mutable public source file was hardlinked. The canceled partial candidates are preserved and excluded from packaging. Final destination is `test-results/solid-fleet-release-dist-final`; `test-results/solid-fleet/static-proof.json` records the static source proof. Build warnings are the existing bundle-size/dynamic-import warnings.

The Windows package and native Steam runtime checks passed. Exact executable: `test-results/astra-build-2026-09-08T17-05-14-062Z/win-unpacked/Nova Swarm.exe`. An isolated packaged run verified all 120 manifest-listed runtime ship assets and the Three.js license, real horizontal/vertical dragging at 1536px, two Hangar/details/menu round trips with continuous four-layer ambience, default-off menu voices and real toggle clicks, Other Modes return, the preserved Colossus carrier asset, and actual gameplay audio handoff. No page errors. The QA app closed after the check. Native report and screenshots: `test-results/solid-fleet/native/`.

Steam server verification at `2026-09-08T17:41:02.959Z`: test branch `sector-continue-test` is build **25192515**, depot 4765071 manifest **4846344151338657407**. Public remains **25169120** and `test-build` remains **23782673**. The complete Cloud configuration block is unchanged. No public deployment, Steamworks configuration change, public post or staged-forum-draft change. Steam automatically retried three HTTP 408 chunk uploads and then confirmed success; no duplicate build was started.

Payload: 410 regular files, 1,982,415,867 bytes. ASAR SHA256 `d35e23cf760784f07adb65fcf48e0e747870a99592a24fb6c75d36452df1fe6d`. Receipt: `docs/solid-fleet-steam-delivery-20260908.json`. All work in this ship/audio batch is delivered; the historical limitations above remain. QA app and source server are closed. There is no new untranslated player-facing text; all eight locale files include the new audio setting.

Source rollback requires reverting the delivery-documentation commit first, then `git revert 9a13182 91fece2`, after clean ownership verification. Previous Steam test build `25189882` is the separate deployment rollback.

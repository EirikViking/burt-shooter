# Astra visual overhaul

## Fourth pass — boss rupture, intermittent hitches and opening experience
- DELIVERED fourth pass: gameplay commit e6fc397acad0c7b7ad02de154e1a840306269617, distinct Windows package test-results/astra-build-2026-09-05T18-46-22-205Z/win-unpacked/Nova Swarm.exe, production dist astra-v8-final-dist (2m01 build). All eight catalogs/80 UI captures, production smoke, controller flow and native control/smoke/perf PASS. Packaged menu/rotation/three starters/codex/800+1920 resize checks PASS and actual captures inspected. Full native 12-scene run, death/restart/relaunch, 25s normal opening, 25s dense and 10s boss recording PASS; sampled actual recording frames inspected. No errors/warnings in the native encounter or matched-frame reports.
- Final matched Daily 1080p (same seed/rules, CPU sampling off, visible throughout, sequential idle-machine runs): p99 opening17.4->17.3ms, dense17.3->17.3, boss17.3->17.2; frames>33.4ms 7->2 / 2->0 / 0->0; maxima150->116.5 / 50->33.3 / 33.3->33.3ms. The116.5ms frame occurs in LEVEL_ENTRY_HOLD with zero enemies/bullets; other opening crossing is33.4ms at floating precision. Native60s avg60.08/min58.82FPS. No claim that all loading hitches are eliminated. Retained JS heap53.09->52.91 / 41.82->42.65 / 34.09->34.58MiB; renderer peak sampled working set592.82->535.13MiB. Menu-art warm load4.50->2.87s; cache-sensitive, not causal proof.
- Native ordinary-input Tactical opening with QA invulnerability: three waves, boss34.565s, first draft52.542s, exact pierce/double_shot/drones, successful selection and continuation. No forced waves/kills/time scaling, not human fun or sales validation. Report: astra-opening-playthrough-v8-final. Daily contract remains unchanged.
- Delivery: test-results/astra-v4-delivery/{README.md,comparison.html,*normal-speed.mp4}, seven actual before/after encounters against preceding visual build, original-baseline comparison linked. HTML images/slider/seven scene buttons/video playback verified. Desktop Nova Swarm - Visual Upgrade.lnk now points to this package. Copied prior15:15 experimental profile only (14 files hash-verified), preserved source and old shortcut. Launched through the actual shortcut; PID38660 responds with Nova Swarm - Arcade Patrol, build-local isolated profile, no Error window/EPIPE. Left open. Steamworks/real saves/Cloud/achievements/live boards untouched; no publication/push/deploy. Final documentation checkpoint recorded by git HEAD and delivery README.
- Final gameplay checkpoint 963d271; production build test-results/astra-v7-final-dist PASS (2m12s). Final package test-results/astra-build-2026-09-05T18-13-35-623Z/win-unpacked/Nova Swarm.exe PASS. Final build's 80 UI captures/all8 languages and production smoke PASS with zero page errors, placeholders, English leaks or failed responses. Warmup integration test confirms all 1,473 eligible profile entries for effective tier10 resolve to prepared texture keys (many entries share hull assets). Full asset integrity:388 registrations,439 codex sources,30x72 turntables PASS. Native final checks pending.
- Controlled repeats exposed variable first-use stalls even with visible windows: warm old package opening max150ms/7 frames>33.4; a colder candidate repeat max916.6ms/17. Do not cherry-pick the favorable first run. CPU trace of another warm candidate had no long tasks and first-wave GPU/idle stalls. Found a definite roster warmup mismatch: scene warmed source-sector enemy tiers while EnemyManager uses effective difficulty (Sector1→8). Corrected normal and elite preparation to the exact manager tier; simulation untouched. Rebuild and final measurements pending.
- Gameplay checkpoint b67089b59f554998ec5cf753d9412915bf8961d7; production build:current and isolated Windows package PASS. Package: test-results/astra-build-2026-09-05T17-33-19-573Z/win-unpacked/Nova Swarm.exe. Original release outputs remain intact.
- Final-source tests passed: all eight catalogs and 80 UI captures, production smoke, all ten boss animations, controller-only flow, debug-unranked, dead-enemy cleanup, healthbar and unchanged idle reruns of boss telegraph/hazard arming. Initial concurrent runs of those two timing checks timed out. Live offer QA found existing chain eligibility rejected the proposed first offer; switched to a permanent support drone, then verified the actual three-card result. No assertion weakened.
- Native automated Tactical opening PASS: 3 waves, first boss 32.53s, first reward 50.77s, Double Shot selected and game continues. Ordinary movement/fire, QA invulnerability, no forced kills or wave skips. This is an aiming script, not human beginner timing or proof of engagement. New harness null-overlay dereference corrected and whole run repeated.
- Matched native Daily 1920x1080, same seed/rules, CPU sampling off, sequential with other test/build/render jobs stopped: opening p99 33.1→17.2ms, frames>33.4ms 27→4, max1100→116.7ms; dense p99 17.3→17.3, count7→7, max316.9→50.1; boss death p99 17.4→17.3, count1→1, max50.1→50.0. No page errors/warnings. Cold/warm cache affects loading (19.40→3.25s). Warm repeat was INVALID: after 33s, regular 1000–1017ms frames with zero CPU long tasks indicate native-window background throttling. Added identical occlusion/background flags to both benchmark launches and visibility telemetry; controlled comparison pending. No claim that all long frames are eliminated.
- Implemented normal Sector 1 as three waves without the extra boss spacing wave; shorter first intro; Sparrow, Pixel Needle and Quasar Fan starter access; real firing-pattern previews; first Tactical choice between piercing, double shot and a permanent support drone. Daily Signal retains original intro/wave rules and exact offer selection. Existing v3 save unlocks/history/XP preserved. Firing-pattern label translated in all eight languages.
- Rebuilt boss death with eight full-size textured hull meshes, a brief reactor core, asymmetric plasma jets, thin pressure waves and cooling debris. Legacy explosion cascade suppressed visually, with exact original particle RNG and allocator state preserved. Victory banner moved below the wreck. Quasar rebuilt in actual Blender as a broad three-cannon crescent, 72 views and matching static fallback; editable quasar-master.blend retained.
- Profile-driven fixes: cache number formatters and trait typography, skip redundant particle geometry, avoid traversing owned enemy visual subtrees, replace threat-action full sorts with equivalent single-pass selection, pre-upload explosion/draft textures before level entry. Fresh-profile native launches now force offline leaderboard reads as well as isolated Steam/Cloud services.
- Passed: legacy explosion RNG/allocator parity and bounds; turntable drag/residency; save integrity; 64 exact Daily offer comparisons and source-sector wave rules; 416 threat-selection comparisons against the original sort; first-run retention scenarios; all eight translation catalogs; release-line. Intermediate production build/package succeeded. Final-source browser/native validation and final package remain pending.
- Intermediate 1920x1080 native profile (66s opening/66s dense/15s boss death) reduced >33.4ms frames 25→5 / 4→1 / 2→2 and maxima 716.7→216.7 / 300.2→183.5 / 233.4→83.4ms. Not identical random normal encounters; do not claim causal final improvement from this alone. Matched Daily measurement still pending. Intermediate package excludes latest threat selection, pre-upload, corrected source-sector opening count and final Quasar/banner changes.
- User rejects boss explosions again, reports intermittent stutter despite good FPS, and requests the in-game recommendations from https://chatgpt.com/s/cx_6a9c3ec7cea88191b7ae08d279a7c889. Shared discussion read in authenticated browser: stronger first three minutes, earlier boss/clear Tactical transformation, three different starters, firing-pattern previews, quieter HUD/announcements, controller/resize stability. Marketing/publication tasks are not part of this game request.
- Explicit clarification: apply gameplay changes to the normal game too, including opening pacing and starter progression. Preserve Daily Signal's current shared challenge rules; earlier blanket mechanics freeze is superseded for this new normal-game work. Still no publishing, Steam changes, real saves/Cloud/achievements/leaderboards.
- Verified clean starting HEAD `88007cba20a86ce8529f7cc56f7b7d8c7813c1dc`, branch `codex/astra-visual-overhaul`, same owned worktree. Fetch/status/branch/log/worktree and AGENTS read. Continue here; preserve all prior packages and desktop launcher until the replacement passes.
- Work underway: native 1080p CPU/frame-time profile spanning first-use combat, dense wave transitions and boss death. No local render/build jobs run during measurement. Stutter metrics include >25/33/50ms frame counts and maximum, not only average FPS. Boss art direction: staged reactor rupture, physical hull sections and cooling debris, replacing the repeated fireball. No game RNG for cosmetic work.

## Third pass — rotating showrooms and complete combat presentation
- Continued from clean `0d512013105028249f987ccd5174d3177ffa0560` on `codex/astra-visual-overhaul`; path, branch, HEAD, status, log, worktree and fetch verified. Original baseline `a0b88d064c31dbc879948babd7751bf52fe7be77` on `codex/forum-129-improvements-20260822`; original dist and every previous release/test package preserved. No inherited work or external publication.
- Integrated 30 painted Blender turntables with 72 views each (flagship 768px, other hulls 384px), real main/hangar dragging, three station interiors, new support/elite/late-enemy art, ten boss archetype paintings across 50 profiles, crisp high-resolution codex dossiers, dimensional hostile bullet cores, animated weapon assemblies, 16-frame reactor explosions, machined menus/HUD and announcement plates. Small gameplay player sprites retain pass-two modeled art. Original imagegen inputs/prompts and editable Blender scenes retained; no paid APIs, purchases or external assets/fonts.
- Latest priority, warning lanes: projected energy fields, directional marks, fixed hazard edges and open ring escape wedges. Existing warning timing, locked aim and collision/safe-lane geometry preserved. Nine centered actual-game warning fixtures inspected; no page errors/warnings. Cosmetic updates consume no gameplay RNG.
- Runtime inspection fixed intercepted hangar mouse input, locked-ship viewing, atlas unload/reacquire races, portrait clipping and narrow long-boss text. Actual main/hangar dragging, rapid scene switching, selected-only residency (1 menu atlas, 0 combat), reduced motion and zero viewer RNG passed. 388 new threat registrations, original 130 hull registrations, 439 decoded codex sources and all 30 complete turntables passed.
- Gameplay checkpoints: `e032cfab10fa3e5ad047af5778c2c52e669bde53` full integration; `a607ceb6d0a3b9384f60488979ed729e90383335` long-boss dossier fix. Native review then found the Ascendant rotation hint overlapping its tier badge. `9c0ac057f388618045810fd1976da85e0a21e6f1` moves that hint above the badge; actual 1920x1080, 1280x720 and 800x600 captures and bounds checks pass. Final rebuilt package and its native verification passed; combat code is unchanged from a607ceb.
- Passed unchanged assertions: all 11 pure checks (deterministic content director, Daily contract/records, mode identity, isolated Steam bridge, projectile lifecycle/defenses, elites, hazards/fairness, i18n); six browser suites (telegraphs, arming, all ten boss animations, healthbar, controller-only flow, debug-unranked); projectile/enemy-death/player-shot visual checks; all 52 codex layout captures; warning lifecycle/interruption/cleanup; exact legacy explosion RNG/allocator parity, dedupe/cap/retirement and reduced flash. The initial projectile stress run overlapped other jobs and failed one >50ms frame; an idle rerun passed unchanged (p95 32.2/p99 39.6/max 41ms in a software-rendered stress fixture). Earlier baseline defects were repaired in pass two, with tests preserved.
- a607ceb full build:current, release-line, eight-language UI and final production smoke passed. Native baseline-v4 and candidate-v4 each passed 12 scenarios including controls, pause, Daily, dense Sector90, boss, destruction, rewards, death, restart and relaunch. Same Electron42.1, 1280x720, Daily seed/rules; sequential measurements with all other render/test jobs stopped. p95/p99 before→after: Daily16.9/17.2→16.9/17.2ms; dense16.9/17.2→17.0/17.1; boss17.0/17.2→16.9/17.0. Retained heap MiB:50.99→53.08,42.14→43.58,34.44→34.97. Candidate native60s perf59.824avg/58.480min FPS, control-smoke and smoke passed with zero game errors/warnings.
- a607ceb actual packaged screenshots and sampled normal-speed video frames inspected: normal-vulnerability opening25.29s, dense25.44s, boss attacks/destruction10.06s, rotating menu13.90s. Silent capture; dense/boss fixtures use isolated QA invulnerability. No human playtest or uninterrupted real-time video review claimed. Final package evidence will supersede these after the small hint fix.
- FINAL: gameplay commit `9c0ac057f388618045810fd1976da85e0a21e6f1`, dist `test-results/astra-v5-final-dist`, separate Windows executable `test-results/astra-build-2026-09-05T15-15-29-108Z/win-unpacked/Nova Swarm.exe`. Final build:current, release-line, all eight UI languages, offline production smoke and native control-smoke/smoke PASS. Native candidate-v5 repeats all 12 scenes/controls/transitions with zero errors/warnings. p95/p99 Daily16.9/17.0ms, dense16.9/17.1, boss16.9/17.1. Native60s average59.9177/min58.8235FPS. Retained heap53.09/41.82/34.09MiB against original50.99/42.14/34.44. Highest sampled renderer507.50→592.82MiB, GPUprocess155.50→189.46MiB. Process peaks including showroom/loading830.69→1265.24MiB renderer,236.93→516.45MiB GPUprocess; these are working sets, not dedicated VRAM.
- Final load observations: menu artwork20.215→12.722s, menu launch14.584→10.015s, stagedSector90 11.165→11.115s. Single runs; preceding same-art candidate needed23.62s for menu artwork, so no startup-speedup claim. Native tests ran sequentially with other render/test/build jobs stopped. Same Daily2026-09-05 seed/rules, same Electron42.1,1280x720; scenario matching, not identical input replay. Baseline uses original production dist through installed Electron, candidate is packaged.
- FINAL evidence: `test-results/astra-v3-delivery/` contains interactive comparisons, current packaged menu gallery, actual source warning fixtures, full native reports, changed-file inventory, launch instructions, tool/rights links and limits. Final packaged videos: normal-vulnerability opening25.31s, dense25.43s, staged boss10.08s and rotating main menu; silent, normal wall-clock speed. Screenshots and sampled video frames inspected; no human playtest/full continuous viewing claimed. Comparison controls and all image sources checked successfully.
- HANDOFF: isolated launcher started PID39628, verified responding with native window `Nova Swarm - Arcade Patrol` from the final build path, build-local `isolated-play-profile`, connected real log handles and no EPIPE. Game left open. Subsequent checkpoint contains delivery/test tooling and this progress record only; packaged gameplay remains9c0ac05. Steamworks, production saves/Cloud/achievements/leaderboards, publishing and existing release outputs untouched. No new untranslated UI strings; intentional proper names/control labels retained. Remaining limits: showroom memory cost/8192 texture-size requirement, reused art families, no hardware matrix/full240-sector endurance or fluent human translation review. Existing bundle and dependency deprecation advisories remain.
- Rollback source command (not run): `git switch codex/forum-129-improvements-20260822`. Preserve any future uncommitted work first.

## Second pass — user-directed visual reset
- Handoff verified: final executable left open as PID 23396, native window `Nova Swarm - Arcade Patrol`, using the build-local `isolated-play-profile`. Real file logging remains connected; no EPIPE recorded. Git worktree clean after checkpointing task-only changes. The ordinary Node/Electron dependency deprecation notice remains in the launch log; gameplay/browser checks report no errors or warnings. No Steamworks, real saves, Cloud, live leaderboards, publication or release outputs touched.
- Delivery complete at `test-results/astra-v2-delivery/`: original/candidate comparisons, nine packaged menu surfaces, exact measurements, test reports, changed-file inventory and launch instructions. Final normal-vulnerability opening video 25.26 seconds, dense combat 25.32 seconds with QA invulnerability, staged boss destruction 4.52 seconds and animated main menu 9.96 seconds; all actual packaged footage at wall-clock speed, silent. Sampled video frames and native menu/hangar/codex/destruction screenshots inspected. No full real-time human review or endurance test claimed. Final isolated player launcher prepared with real log-file handles; launch verification is the remaining handoff step.
- Final gameplay checkpoint `cc4b6fa8755020a43119e18b3e4fda2b38287080`. Build: `test-results/astra-v3-explosion-dist`; separate Windows package `test-results/astra-build-2026-09-05T09-12-47-386Z/win-unpacked/Nova Swarm.exe`. Production build, release-line, eight-locale UI, smoke and boss telegraphs PASS. Final native candidate-v3 passes all 12 scenarios/controls/transitions with zero errors/warnings. Same Daily seed/rules as original baseline-v2. p95/p99 Daily 16.9/17.2 ms; dense Sector 90 17.0/17.2; boss 17.0/17.2. Separate 60-second native perf PASS average 59.9192 FPS / minimum 58.4795; control-smoke and smoke PASS. Normal-vulnerability recording and native menu gallery in progress. Dense and boss-death normal-speed videos captured.
- Final destruction review inspected actual boss, ordinary enemy and player-hit effects. Native destruction image also inspected; real boss fragments and layered fire/smoke read clearly. Legacy elite tendrils removed; victory sparks remain small. Native retained JS heap: Daily 59.19 / dense 42.24 / boss 33.83 MiB. Highest sampled renderer working set 478.68 -> 629.38 MiB; GPU-process 172.30 -> 208.39 MiB. Richer cached artwork costs memory; effects/world textures are bounded and frame target remains intact. These are process working sets, not dedicated VRAM.
- Latest request: replace terrible boss/ordinary explosions. Added an original Blender 24-frame turbulent combustion atlas, bounded 18-effect renderer, one reactor pressure front, longer boss armor breakup, reduced legacy death diagrams and quiet victory sparks. Two offline prototypes rejected; actual boss death inspected in iterations 2 and final-browser, then removed duplicate arcs and screen-wide victory fireballs. Final staged ordinary/player/boss review pending. RNG/particle allocator parity against committed 682264e PASS exactly; reduced motion, dedupe, bounds and retirement PASS. Existing death-feedback readability assertions PASS unchanged.
- Native original a0b88d0 baseline-v2 passed all 12 checks. Pre-explosion candidate-v2 measured near 60 FPS but suite failed when staged native navigation lost focus before first wave. Harness now applies existing QA focus suppression before wave readiness; game pause behavior unchanged. Custom-protocol service-worker registration warning guarded. New explosion package and final candidate run required; do not label candidate-v2 passed.
- Final integration inspected in actual running game: `astra-menu-final`, `astra-hangar-final`, `astra-v3-final-fleet`. All 30 showroom ships, flagship hover/reflections/arcs, 50 boss presentations, 48 worlds and 14 menu/overlay material treatments are integrated. Compact hangar banner overlap and oversized Ascendant previews corrected. Editable station files pack their source planet image.
- Final browser checks PASS: all 10 boss animations, 52 codex layout captures, 11 codex lore scenarios across eight locales, controller-only flow, unlock reveal, ranked parity/debug-unranked, locale-cache/halo regressions, all 130 decoded hull registrations and unchanged original 75-hull visual/RNG check. Runtime animation/reduced motion/RNG and bounded planet unloading PASS with zero errors/warnings.
- Next: production build in a new output folder, Windows package, same-day native baseline/candidate frame-time and memory comparisons, native control/smoke and normal-speed gameplay evidence. Prior first-pass performance is historical, not the new candidate result.
- `5dff6ca` production build passed; first second-pass package is intermediate. Production smoke passes with zero routine logs, warnings, errors or bad responses; production boss telegraph passes unchanged (dev-server attempts timed out). Manual Japanese result-screen inspection found inherited uppercase-before-translation, untranslated end-state, stale LEVEL/SECTOR and Global-status patterns, and romanized offline messages. Corrected these visible phrases across all seven translated locales; final rebuild required.
- Current UI pass: shared machined console material across 14 menu/overlay modules, larger two-column codex dossier, full new ship/boss/world presentation mappings. Eleven codex lore/layout scenarios pass across all 8 locales. New baseline menu captures: `test-results/astra-menu-before-v3`; current captures are intermediate until full asset pack and shader review.
- User additionally requests exceptional hangar/main-menu ships, lighting/electrical details and menu animation. Added 30 angled showroom renders plus a more detailed flagship, projected light anchors, slow hover/banking, a bounded menu-only reflection filter, restrained engine arcs and reduced-motion handling. Inspect real running integration next. Third interior render iteration complete; rejected floor-heavy first view and bland second vista.
- Checkpoint `205194b` contains logging, debug policy, locale-cache, halo, SFX and boss-fixture fixes. Remaining second-pass changes are task-owned WIP. Native second-pass performance/build/video validation is still pending; do not reuse first-pass numbers as new results.
- Asset registration test caught a 2-pixel antialias shrink at the Railbreaker nose. Packer now refits the surviving alpha silhouette without adding fake pixels; rerun full 130-hull verification. Original 75-hull test remains unchanged.
- Continuation starts from clean `a24b372a3eb754867b9288333ca00a86eaf59999` on the same experimental branch. Original pre-overhaul baseline remains `a0b88d0`; all previous packages and evidence remain intact.
- User rejected the clean but flat presentation and requests much richer, detailed Blender ships and improvements throughout, plus all known issues fixed. New environments should change roughly every five sectors.
- Direction: intricate industrial spacecraft, sculpted armored fuselages, exposed machinery, deep engine ducts, metallic edge lighting, layered planetary worlds and richer cinematic effects. Validate a fully integrated encounter before fleet-wide expansion.
- Issue inventory: stale policy after marking a run unranked; obsolete boss-animation fixture timing; language-switch briefing cache; missing point-defense SFX alias; absent endless-halo cache lookup; EPIPE logging robustness. Existing assertions must remain at least as strong.
- Integrated second-pass encounter inspected in `test-results/astra-v2-encounter`: new player, first modeled boss and detailed cloud/continent world. Iterated ship twice and boss armor again after review; reducing decorative outlines and adding modeled animated weapon/armor parts. These development captures ran alongside Blender and are excluded from performance comparisons.
- Full GPU render batch in progress: 50 bosses (10 structural archetypes, 5 tiers), 30 players, 50 enemies, 48 planetary scenes. Every five sectors changes the world through 240 sectors, then cycles. Offline procedural sources; no paid services or imported assets.
- Fix evidence: debug-unranked passes with zero score GET/POST. Logging-pipe regression passes an actual disconnected child-process pipe and confirms unrelated errors still propagate. Boss-animation now exercises a real warning token; all 10 animation assertions pass, but this run saw a mid-pack image-decode warning and must be rerun with stable assets. Packer now replaces files atomically. Pure persistence, determinism and Steam bridge checks pass. Language switch, halo sizing, full ranked/Daily/UI and native tests remain under verification.

Original prompt: Transform Nova Swarm's actual PixiJS/Electron/Vite presentation; preserve all gameplay and deterministic/ranked rules; capture baseline, iterate in-game, validate and deliver an isolated Windows test build. No publication or Steam activity.
- Original branch: `codex/forum-129-improvements-20260822`
- Starting commit: `a0b88d064c31dbc879948babd7751bf52fe7be77`
- Experimental branch: `codex/astra-visual-overhaul`
- Preflight: clean worktree; fetch completed; current branch has no upstream. Current checkout explicitly chosen by owner as baseline. Root AGENTS.md read. Stay in this folder; no other agents.
- Tools: installed Node, npm, Python 3.12, FFmpeg, local Playwright, Electron and Vite. Blender not on PATH. No paid services or asset purchases.
- Isolation: browser contexts block external requests; Electron fresh-profile mode plus explicit task-local user data disables Steam services.
- Baseline capture complete: `test-results/astra-baseline/` contains loaded menu, opening, ordinary combat, Sector 30, pause, boss, destruction, Tactical Draft and death. Browser requests restricted to localhost. Boss death and test invulnerability are harness staging, not normal-play claims. Query seed is not a supported general run seed; scenes are matched scenarios, not identical replays.
- Baseline browser 1280x720: median 16.7 ms, p95 ordinary 17.1 ms / dense 17.0 ms / boss 17.0 ms; heap 121.6 / 149.6 / 127.9 MiB. Short 6-second samples, not a sustained stress verdict.
- Baseline release-line, localization, policy, backdrop and boss-roster checks passed. Production `build:current` passed (6m47s, existing oversized chunk warning). Vite dev server failed its local CommonJS policy import; capture used an export-syntax-only transport adapter. Narrow development-server adapter added afterward; no policy behavior changes.
- Direction: orbital foundry; cool alloy hulls with restrained colored markings, coherent planetary depth, quiet negative space, reduced decorative auras. Prototype is now being captured in the game before extending to menus and effects.
- Integrated slice inspected, then expanded to 25 original modeled player hulls and 50 enemy hulls, three Blender orbital plates, menu centerpiece, clean menu panels, HUD plaques and bounded hull breakup. Existing bosses, special Ascendant hulls and late Mayhem identities retained. Original canvas dimensions and alpha footprints matched by packing; registration receipt in `docs/astra-models/registration.json`.
- Actual captures: `test-results/astra-modeled-slice/` and `test-results/astra-fleet/`. New fleet is visibly brighter and easier to distinguish in combat. First menu model was too plain at close range; refining its material and service detail. Initial SVG backdrop prototype rejected.
- Baseline existing failure discovered using unchanged production `dist` at a0b88d0: `check-debug-run-unranked` fails result-screen rank/submission-state assertions (zero score posts, zero achievements, but 2 mocked score reads). Report `test-results/debug-run-unranked-2026-09-04T22-05-26-435Z/report.json`. Test unchanged. Native baseline perf/control readiness also timed out; retry pending with longer readiness timeout.
- Pure checks passed: content-director determinism, Daily Signal contract and records, run-mode identity. Daily records deliberately tests quota failure and logs it as expected.
- Blender 4.5.4 portable downloaded from official distribution after disk cleanup; artwork is original, no external assets or paid services. Official Blender license permits commercial rendered artwork. Existing font licenses retained.
- Checkpoint `c4959ea`: integrated art and editable sources. Candidate production build succeeded in 1m48s at `test-results/astra-candidate-dist`; original baseline `dist` intentionally retained. Separate packaged executable built at `test-results/astra-build-2026-09-04T22-19-58-099Z/win-unpacked/Nova Swarm.exe` (first candidate; draft field refinement pending next build).
- Native 60-second baseline: average 60.013 FPS, minimum 59.524; test failed solely on existing missing `tactical_point_defense` SFX warning. Packaged candidate native perf check PASSED, average 59.883 FPS, minimum 59.524. Full logs/reports in `test-results/astra-native-{baseline,candidate}-perf-smoke/`.
- A direct PowerShell GUI launch lost stdout, producing EPIPE dialogs. User asked to clear them; the verified test process was stopped and no Error/Nova Swarm windows remained. `scripts/astra-native-check.mjs` now holds real log-file handles until process exit; subsequent native run finished cleanly, with no dialogs. Do not launch Electron through a short-lived redirected GUI shell again.
- Passed: all 8 i18n UI languages (no placeholder/leak/page errors), controller flow, 75-sprite registration/RNG/debris integrity, generated rosters/art/variety, Steam bridge isolation, projectile lifecycle and defense rules, standard skill client with compositor screenshot (canvas toDataURL is black with Pixi's cleared drawing buffer).
- Boss-animation production test reaches all 10 bosses but its fixture produces zero shutter/charge-ray/servo-spark activity; the side-panel X span is also zero but is not itself an assertion. Baseline comparison completed with the same zero-charge failure. First dev attempt had a readiness timeout. No test changes. Boss telegraph, collision stress and projectile readability passed (readability requires the dev server for dynamic source imports). Debug-unranked retains the baseline result-state failure.
- Final gameplay checkpoint `4f0874b` removes repeated planets from draft-card backgrounds. Final Windows x64 package: `test-results/astra-build-2026-09-04T22-35-48-766Z/win-unpacked/Nova Swarm.exe`. Production build and separate packaging passed; original dist and release outputs retained.
- Native baseline and final package both passed 12 encounter/control captures at asserted 1280x720: menu, opening, shooting/movement, pause/resume, Daily Signal, Sector 90, boss, destruction, rewards, death, Enter restart and menu relaunch. Reports: `test-results/astra-desktop-{baseline,candidate}/report.json`. Invalid earlier capture setups are retained under explicitly named invalid folders; native debug URLs were ignored and window size was outer bounds, so those measurements are excluded.
- Final matched native frame p95/p99 (before -> after): Daily 17.0/17.2 -> 16.9/17.1 ms; Sector 90 17.0/17.2 -> 16.9/17.1; boss 16.9/17.1 -> 16.9/17.1. Retained JS heap MiB: 43.80 -> 43.27; 41.40 -> 42.29; 34.42 -> 33.41. Samples 20/20/12 seconds. Same Daily seed and rules hash; scenario-matched runs are not identical input replays. Dense counts peaked at 17 enemies, 70/44 bullets before/after.
- Loading observations: menu launch 7.352 -> 7.463 seconds; staged Sector 90 readiness 11.099 -> 11.426 seconds. Single-run measurements. Final packaged 60-second native perf passed, 60.0158 FPS average / 59.5238 minimum, no warnings/errors. Final native control-smoke and smoke both passed.
- Full offline browser smoke passed, no page errors or failed responses. Eight-locale UI check passed, but manual inspection found stale German briefing values after switching to Chinese; reproduced on baseline. Existing debug-unranked and boss-animation failures remain. No assertions weakened or unrelated policy changes made.
- Two real packaged recordings: normal opening gameplay (26.31 seconds, normal vulnerability, ending with 2 lives) and dense combat (25.43 seconds, QA invulnerability), 1280x720 H.264 30 FPS output at original wall-clock speed, silent. Native screenshots inspected across all major surfaces; video frames sampled for readability/continuity. No human playtest or full real-time video viewing is claimed.
- Delivery generator `node scripts/astra-delivery.mjs` creates local comparison HTML, videos, reports, changed-file inventory and an isolated launcher with real log-file handles. Delivery folder `test-results/astra-delivery/`. No player-facing strings added; no new imported fonts/art, paid services, publishing or Steam changes.

- Final inspection caught a live hangar resize defect missed by the first bounds assertion: the canvas resized while content stayed in its original coordinates. ShipSelectScene now fits its live coordinate frame without rebuilding atlases or losing selection/rotation. Source captures at 1920x1080, 800x600 and 1280x720 plus actual pointer launches pass; visually inspected 800 hangar/launch images. Packaged QA now also checks footer and launch button inside the physical viewport. New package required before delivery.

- 2026-09-05 Steam upload requested and completed: BuildID 25144844 on sector-continue-test, packaged source e6fc397; public 24879347 unchanged. Same released Cloud and leaderboard identities. Native modules loaded, live read probes blocked by desktop Steam user not logged on; no synthetic submissions. Receipt: docs/astra-steam-upload-20260905.md.

- V5 continuation 2026-09-06: starting source 95173e42b714d3f748a7b341d7ad559999ca4193; checkpoint 4631959. Integrated 227 original ImageGen hulls with exact prior canvas/alpha registration (18.09 MiB decoded), 1,780 unique digit-free enemy names with all other profile fields unchanged, 111 support callsigns, 12 drone designs, 40 actual rank badges and a Blender-rendered animated dock tug. Source sheets, prompts, blend file and packing scripts retained.
- V5 visual inspection found real connected-arc chords across boss hulls and warning fields. Explicit path starts remove them; countdown radius/progress/release timing unchanged, thinner amber stroke. Matched renderer captures under astra-warning-v5-before-arcs / after-arcs pass all nine scenes. Full fleet alpha bounds and gameplay profile parity pass; faint alpha haze in source 203 normalized before packing.
- V5 lifecycle check passes 200 drone disposals and six menu/Codex reentries; owned resources released, shared textures retained. Earlier 20-minute Pure baseline soak completed without crash but only reached Sector 7; cannot dismiss the reported 400k/late-sector black screen. Concurrent workloads invalidate those soak frame outliers as clean benchmarks.
- V5 build:current passed all catalog/policy/i18n checks and built separate astra-v5-candidate-dist in 2m11s. Prior eight-locale UI checks passed. Source boss warning lifecycle, hazards and drone clarity pass; boss-telegraph source fixture timed out on its 10-second boss readiness gate, pending packaged retry. No assertions weakened.
- Marketing recording prototype captures actual normal rules with sound and 180ms automated keyboard decisions, no invulnerability or synthetic damage. One 90-second trial legitimately defeated the opening boss and reached Sector 2 with three lives. It is explicitly an automated pilot, not a human performance; final candidate footage is still pending.

- V5 final runtime checkpoint `8ebcd6e`; separate package `astra-build-2026-09-06T00-36-04-502Z`. Twelve native encounter/control checks and the full native menu/rotation suite pass. Final eight-locale UI, controller, production boss telegraph and 60-second performance smoke pass (60.08 FPS average). Scout Codex immediate-write assertion fails identically on unchanged `95173e4`; not weakened. Raw timing outliers, including slow concurrent-staging startup, remain in the delivery report.
- Final normal-rules footage: 110-second Tactical run with a real first-boss defeat, and 90-second Overrun Sector 51 run; 12/15 automated phase decisions, three lives remaining, captured game audio. Two 1080p30 H.264/AAC trailer drafts and eight actual screenshots prepared, audio mastered to about -18 LUFS. No human performance or superiority over existing trailers claimed; store media awaits approval.
- Steam test upload completed: BuildID `25147283`, depot manifest `8952128980673134231`, branch `sector-continue-test`. Published app info and live Steamworks page confirm; default `24879347` unchanged. Existing Cloud/boards preserved; package archive hash verified. Receipt `docs/astra-steam-upload-20260906-v5.md`.
- Feedback review/decisions: `docs/astra-v5-feedback-decisions.md`. Delivery and comparisons: `test-results/astra-v5-delivery`; media ZIP: `test-results/Nova-Swarm-Astra-V5-Steam-Media-Review-20260906.zip`. Desktop Visual Upgrade shortcut now targets the final isolated package; original shortcut backup retained in delivery folder.
- Final packaged 20-minute late-sector soak PASSED, staged Sector 31 through Sector 35, no captured errors. Ordinary keyboard fire and normal wall-clock timing, QA invulnerability only; this does not reproduce an accumulated 400k-score journey or prove that report fixed. Concurrent media/upload work makes this a stability observation, not a clean frame-time benchmark. Report: `test-results/astra-endurance-v5-final-late/report.json`.

- Astra V6 requested menu/Overrun/first-flight pass. Baseline 7358d00757869543df9d1c14110cbea8e256c075, clean same branch; fetch and release-line passed. Menu idle frame rotation disabled, small service craft removed, landing lights added. Built-in ImageGen portal plus existing Blender ship portraits now personalize all milestone celebrations and the menu unlock. Original baked fanfare replaces the old coronation cue. New starter-hangar information is localized in all eight languages and does not delay play.
- V6 focused presentation checks pass seven milestones, new-pilot cue, reduced motion, narrow layouts and menu unlock; actual screenshots inspected. Eight-language UI/i18n pass. Source-regex score-bonus assertion reproduced on clean baseline. Earlier controller-first-run and broad pointer-milestone fixture timeouts remain under investigation; no assertion changes. Separate production build in progress; Steam upload not yet performed for V6.
- V6 native inspection caught stale entrance-animation coordinates after a 1920x1080 resize. Replaced position animation with fades and added rapid-resize bounds checks at both 1920 and 1280. Broader 18-combination Overrun confirmation suite passed; new release candidate pending final native performance and Steam test upload. Previous candidate remains preserved.
- User added last-Hangar-ship menu request. Menu now reads the existing persisted/Cloud-backed Hangar choice and resolves its showroom, turntable and emitters; temporary loaners do not replace it. Menu unlock portrait uses that ship too. Preceding native package passed menu resize and all celebrations plus full menu QA, but was not uploaded and is superseded by this final selection change.
- Final native visual review caught an inherited ordering issue: rank-up command banners (z=9960) rendered above the paused Overrun layer (z=9600). Raised the ceremony layer to 12000, below settings/terminal overlays, preserving score and toast timing. Native screenshot evidence retained in astra-v6-packaged-last-ship/overrun-30.png. Rebuilding before any Steam upload.

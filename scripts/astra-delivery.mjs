import {readFileSync,writeFileSync,mkdirSync,copyFileSync} from 'node:fs';
import {execFileSync} from 'node:child_process';
import path from 'node:path';
import assert from 'node:assert/strict';
const root=process.cwd(),out=path.resolve('test-results/astra-delivery');mkdirSync(out,{recursive:true});
const load=p=>JSON.parse(readFileSync(p,'utf8'));
const before=load('test-results/astra-desktop-baseline/report.json'),after=load('test-results/astra-desktop-candidate/report.json');
const opening=load('test-results/astra-desktop-candidate-opening/report.json');
const nativePerf=load('test-results/astra-native-final-perf-smoke/report.json');
for(const r of [before,after,opening,nativePerf])assert.equal(r.status,'passed');
assert.equal(before.dailyContract.seed,after.dailyContract.seed);
assert.equal(before.dailyContract.rulesHash,after.dailyContract.rulesHash);
const build=load('test-results/astra-build-location.json');
const head=execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();
const branch=execFileSync('git',['branch','--show-current'],{encoding:'utf8'}).trim();
const scenes=[['01-menu','Menu'],['04-combat','Combat'],['05-daily','Daily challenge'],['06-dense','Dense combat'],['07-boss','Boss'],['09-rewards','Rewards'],['10-death','Game over']];
for(const [id]of scenes)for(const label of ['baseline','candidate'])copyFileSync(`test-results/astra-desktop-${label}/${id}.png`,path.join(out,`${label}-${id}.png`));
copyFileSync(opening.recording.video,path.join(out,'gameplay-normal-speed.mp4'));
copyFileSync(after.recording.video,path.join(out,'dense-gameplay-normal-speed.mp4'));
for(const label of ['baseline','candidate','candidate-opening'])copyFileSync(`test-results/astra-desktop-${label}/report.json`,path.join(out,`${label}-report.json`));
copyFileSync('test-results/astra-native-final-perf-smoke/report.json',path.join(out,'native-perf-report.json'));
writeFileSync(path.join(out,'files-changed.txt'),execFileSync('git',['diff','--name-only','a0b88d064c31dbc879948babd7751bf52fe7be77',head],{encoding:'utf8'}));
const num=n=>Number(n).toFixed(2);
const rows=['daily-seeded','sector-90-dense','boss'].map(name=>{const b=before.performance.find(r=>r.name===name),a=after.performance.find(r=>r.name===name);return{name,before:b,after:a};});
const launch=path.join(build.output,'launch.mjs');
writeFileSync(launch,`import {spawn} from 'node:child_process';\nimport {openSync,mkdirSync,writeFileSync} from 'node:fs';\nimport path from 'node:path';\nimport {fileURLToPath} from 'node:url';\nconst root=path.dirname(fileURLToPath(import.meta.url));\nconst profile=path.join(root,'isolated-play-profile');mkdirSync(profile,{recursive:true});\nconst log=openSync(path.join(root,'play.log'),'a');\nconst child=spawn(path.join(root,'win-unpacked','Nova Swarm.exe'),['--nova-fresh-profile','--windowed'],{cwd:root,env:{...process.env,NOVA_SWARM_USER_DATA_DIR:profile,NOVA_SWARM_FRESH_PROFILE:'1',NOVA_SWARM_WINDOWED:'1'},stdio:['ignore',log,log],windowsHide:false,detached:true});\nwriteFileSync(path.join(root,'play-process.json'),JSON.stringify({pid:child.pid,profile,startedAt:new Date().toISOString()}));child.unref();\n`);
const performanceTable=['| Scenario | Before p95 / p99 ms | After p95 / p99 ms | Before / after retained JS heap MiB |','|---|---:|---:|---:|',...rows.map(r=>`| ${r.name} | ${num(r.before.p95Ms)} / ${num(r.before.p99Ms)} | ${num(r.after.p95Ms)} / ${num(r.after.p99Ms)} | ${num(r.before.retainedHeapMiB)} / ${num(r.after.retainedHeapMiB)} |`)].join('\n');
const md=`# Nova Swarm — Astra visual experiment\n\nPlayable Windows x64 package, local only. Original modeled fleet (25 player / 50 enemy hulls), orbital backgrounds, menu centerpiece and panels, HUD materials, quieter boss decoration, and bounded hull fragments are integrated in the game.\n\n## Play\n\nRun in PowerShell:\n\n\`\`\`powershell\nnode "${launch}"\n\`\`\`\n\nExecutable: [Nova Swarm.exe](<${build.executable.replaceAll('\\','/')}>)\n\nUse the launcher to retain the isolated test profile and disabled Steam services. It writes only inside this build folder. Controls remain WASD/arrows, Space to fire, and P to pause; other controls are unchanged.\n\n## Provenance\n\n- Original branch: codex/forum-129-improvements-20260822\n- Exact starting commit: a0b88d064c31dbc879948babd7751bf52fe7be77\n- Experimental branch: ${branch}\n- Final repository commit: ${head}\n- Packaged gameplay code: ${after.checks[0].state.gitSha}; subsequent commits record validation and launch tooling.\n- Package: ${build.output}\n- [Full changed-file inventory](files-changed.txt). Original release outputs and original production dist preserved. No push, pull, merge, rebase, publication, Steam upload or Steamworks settings changes.\n- Return to the original source: \`git switch codex/forum-129-improvements-20260822\` (the experimental build remains available).\n\n## Actual game evidence\n\n[Interactive before/after comparisons](comparison.html) · [Normal-speed opening gameplay recording](gameplay-normal-speed.mp4)\n\nAll pictures and video are captured from the running game. Candidate evidence comes from the packaged executable, not Blender renders. The opening video uses scripted keyboard input with normal lives and vulnerability in an isolated practice run, at normal wall-clock speed. A separate dense-combat video uses QA invulnerability. Both recordings are silent. Game audio is unchanged. Boss defeats and rewards are staged for coverage.\n\n## Performance\n\n${performanceTable}\n\nSame Electron ${after.runtime.electron}, 1280×720 content viewport and graphics settings, measured sequentially. Heap is sampled after explicit collection outside frame measurements. Daily contract: ${after.dailyContract.dailyKey}, seed \`${after.dailyContract.seed}\`, rules \`${after.dailyContract.rulesHash}\`. Daily content matches; these are not full deterministic input replays, and incidental randomness remains live. Dense combat uses the same Sector 90 scenario. Population and process-memory samples are retained in [baseline-report.json](baseline-report.json) and [candidate-report.json](candidate-report.json). The frame sample sizes are 20 seconds for daily and dense combat, and 12 seconds for the boss.\n\nMenu-to-controllable loading: ${before.performance.find(p=>p.name==='menu-to-controllable')?.loadMs} ms before / ${after.performance.find(p=>p.name==='menu-to-controllable')?.loadMs} ms after. Sector 90 loading: ${before.performance.find(p=>p.name==='dense-load')?.loadMs} ms / ${after.performance.find(p=>p.name==='dense-load')?.loadMs} ms. These are single-run observations, not statistical load-time guarantees.\n\nThe separate 60-second native performance check passed on the packaged candidate: minimum ${num(nativePerf.minFps)} FPS, average ${num(nativePerf.avgFps)} FPS (baseline minimum 59.524, average 60.013; baseline check failed only on its existing missing-SFX warning).\n\n## Validation and limits\n\nPassed: release-line; build:current and its full preflight; i18n; i18n-ui in all eight locales; full offline smoke; controller flow; Steam bridge isolation; deterministic content director; Daily Signal contract and records; run-mode identity; projectile lifecycle, defense and readability; collision stress; boss telegraph; generated rosters/art/variety; 75-hull registration and no-RNG bounded-debris checks. The final native smoke, control-smoke and 60-second perf-smoke checks passed. Native packaged checks cover assets, launch, movement, shooting, pause, resume, daily mode, dense combat, boss, rewards, death and restart. Existing assertions were not weakened.\n\nExisting failures reproduced on both starting build and experiment: boss-animation fixture has zero shutter/charge-ray/servo-spark activity; debug-unranked result-screen state incorrectly reports ranked/submission eligibility (isolated checks sent zero score posts and unlocked zero achievements). Native baseline also logs an unknown tactical_point_defense SFX. Smoke reports the existing endless-signal-halo cache warning. These remain outside this presentation change.\n\nManual QA also found stale German briefing values after switching to Chinese in the reused language-test context, reproduced in the baseline. Automated i18n checks did not detect this. No player-facing text or translations were added. No human playtest or long endurance run is claimed; special Ascendant and late-game hull identities remain based on the existing artwork.\n\nTools, rights and regeneration: [art notes](<${path.join(root,'docs/astra-art-notes.md').replaceAll('\\','/')}>) and editable Blender sources under docs/astra-models. No paid APIs, purchases or third-party art imports.\n`;
writeFileSync(path.join(out,'README.md'),md);
writeFileSync(path.join(out,'performance.json'),JSON.stringify({before:before.performance,after:after.performance,rows},null,2));
const cards=scenes.map(([id,name])=>`<button data-id="${id}">${name}</button>`).join('');
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Nova Swarm · Astra comparison</title><style>*{box-sizing:border-box}body{margin:0;background:#080f18;color:#e6eff3;font:16px system-ui}main{max-width:1440px;margin:auto;padding:36px 24px}h1{font-size:34px;margin:8px 0 12px}p{color:#9fb6c3;line-height:1.6}small{color:#9de0e4;letter-spacing:.16em}nav{display:flex;gap:8px;flex-wrap:wrap;margin:24px 0 16px}button{font:inherit;padding:10px 18px;color:#bed0d9;background:#142333;border:1px solid #30495a;border-radius:4px;cursor:pointer}button.active{color:#091823;background:#a6e3e8}#stage{position:relative;aspect-ratio:16/9;background:#000;overflow:hidden;border:1px solid #344852}#stage img{position:absolute;inset:0;width:100%;height:100%;object-fit:contain}#after{clip-path:inset(0 0 0 50%)}#line{position:absolute;top:0;bottom:0;left:50%;width:2px;background:#fff;pointer-events:none}.label{position:absolute;top:14px;padding:6px 10px;background:#08131ee8;font-size:12px;letter-spacing:.1em}.left{left:14px}.right{right:14px}input{width:100%;margin:20px 0;accent-color:#a6e3e8}video{width:100%;max-height:80vh;background:#000}a{color:#a6e3e8}footer{margin-top:28px;color:#9fb6c3}</style><main><small>CAPTURED IN THE RUNNING GAME · LOCAL EXPERIMENT</small><h1>Nova Swarm / Astra</h1><p>Drag the divider to compare the starting build with the packaged experiment. Scenes share resolution and scenario; this is not an identical replay.</p><nav>${cards}</nav><div id="stage"><img id="before" alt="Starting game"><img id="after" alt="Packaged experiment"><div id="line"></div><span class="label left">BEFORE · a0b88d0</span><span class="label right">AFTER · ${after.checks[0].state.gitSha}</span></div><input aria-label="Comparison divider" id="slider" type="range" min="0" max="100" value="50"><p>Normal-speed packaged gameplay · scripted keyboard input · isolated practice profile · normal vulnerability · silent capture</p><video controls preload="metadata" src="gameplay-normal-speed.mp4"></video><p><a href="dense-gameplay-normal-speed.mp4">Also view the dense-combat stress recording (QA invulnerability)</a></p><footer><a href="README.md">Build, launch instructions, measurements, tests and limitations</a></footer></main><script>const before=document.querySelector('#before'),after=document.querySelector('#after'),line=document.querySelector('#line'),slider=document.querySelector('#slider');function select(id){before.src='baseline-'+id+'.png';after.src='candidate-'+id+'.png';document.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.id===id))}document.querySelectorAll('button').forEach(b=>b.onclick=()=>select(b.dataset.id));slider.oninput=()=>{after.style.clipPath='inset(0 0 0 '+slider.value+'%)';line.style.left=slider.value+'%'};select('01-menu');</script></html>`;
writeFileSync(path.join(out,'comparison.html'),html);console.log(JSON.stringify({out,launch,head,branch,build:build.output}));

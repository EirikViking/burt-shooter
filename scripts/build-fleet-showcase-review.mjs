import fs from 'node:fs/promises';
import sharp from 'sharp';
import {ShipData} from '../src/config/ShipData.js';
const root='docs/fleet-showcase',rows=[];
for(let n=1;n<=30;n++){
 const id=String(n).padStart(2,'0'),d=JSON.parse(await fs.readFile(`${root}/ships/${id}/asset-cost.json`));
 rows.push({...d,id,name:ShipData.find(s=>s.textureIndex===n-1).name});
}
await fs.writeFile(`${root}/fleet.json`,JSON.stringify(rows,null,2));
// Contact sheet of actual runtime ship buffers. Full, unretouched game captures
// remain available beside these transparent buffers; no beauty-render substitution.
const tiles=[];
for(let i=0;i<rows.length;i++){
 const r=rows[i],x=i%5*320,y=Math.floor(i/5)*300;
 const png=await sharp(`${root}/evidence/${r.id}-runtime-buffer.png`).trim().resize(300,245,{fit:'inside'}).png().toBuffer();const m=await sharp(png).metadata();
 tiles.push({input:png,left:x+Math.round((320-m.width)/2),top:y+10+Math.round((245-m.height)/2)});
 const text=`<svg width="320" height="40"><text x="160" y="24" text-anchor="middle" fill="#dbe7ee" font-family="Arial" font-size="14">${r.id} ${r.name}</text></svg>`;
 tiles.push({input:Buffer.from(text),left:x,top:y+255});
}
await sharp({create:{width:1600,height:1800,channels:4,background:'#16222d'}}).composite(tiles).png().toFile(`${root}/runtime-contact-sheet.png`);
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Nova Swarm - complete showcase fleet</title><style>*{box-sizing:border-box}body{margin:0;background:#0a121b;color:#dce8f0;font:16px/1.6 system-ui}main{max-width:1500px;margin:auto;padding:36px 28px}h1{font-size:40px;line-height:1.15}h2{margin-top:42px}a{color:#81c8f0}p{max-width:960px;color:#b9cad6}nav{display:flex;gap:20px;flex-wrap:wrap;margin:24px 0}.tag{color:#80c8e8;letter-spacing:2px;font-size:12px}button,select{font:inherit;padding:10px;background:#1a3042;color:#e7f3ff;border:1px solid #527086;border-radius:4px}button{cursor:pointer}.pair{display:grid;grid-template-columns:1fr 1fr;gap:14px}img,video{width:100%;display:block}figure{margin:0;background:#12202d}figcaption{padding:12px}.gallery{display:grid;grid-template-columns:repeat(5,1fr);gap:12px}.gallery button{padding:6px}.gallery img{aspect-ratio:1;object-fit:contain}.note{padding:20px;border-left:3px solid #77bcd9;background:#14222f}table{border-collapse:collapse}td,th{padding:8px 18px;border-bottom:1px solid #314452;text-align:left}@media(max-width:850px){.pair{grid-template-columns:1fr}.gallery{grid-template-columns:repeat(3,1fr)}}</style><main><div class="tag">ALL 30 PLAYABLE SHIPS · INTERACTIVE SHOWCASE FLEET</div><h1>Nova Swarm: maintained spacecraft</h1><p>The approved Sparrow construction and renderer are extended across the fleet, preserving each ship's hull plan, colors and gameplay identity. Every combat sprite remains unchanged. Hangar framing uses a modest enlargement with a consistent size cap so large ships fit; the surrounding interface is retained.</p><nav><a href="/?skipIntro=1">Open the game</a><a href="#fleet">Fleet overview</a><a href="#rotation">Runtime video</a><a href="#technical">Measured costs</a><a href="README.md">Full report and reproduction</a></nav><h2 id="selected">Actual runtime inspection</h2><p>Choose any ship, then inspect its genuine main-menu and hangar captures. These are unretouched screenshots from the running game.</p><nav><select id="ship">${rows.map(r=>`<option value="${r.id}">${r.id} ${r.name}</option>`).join('')}</select><button data-pose="hero">Hero</button><button data-pose="side">Side</button><button data-pose="rear">Rear</button><button data-pose="underside">Underside</button></nav><div class="pair"><figure><a id="mainLink"><img id="main"></a><figcaption id="caption"></figcaption></figure><figure><a id="hangarLink"><img id="hangar"></a><figcaption>Actual hangar - selected model, larger framing</figcaption></figure></div><h2 id="fleet">Complete runtime fleet</h2><p>These thumbnails are the actual game's transparent ship buffers, arranged on a neutral background for comparison. Click to open the corresponding full game screenshots above. They are not Blender renders.</p><div class="gallery">${rows.map(r=>`<button data-ship="${r.id}"><img loading="lazy" src="evidence/${r.id}-runtime-buffer.png">${r.name}</button>`).join('')}</div><p><a href="runtime-contact-sheet.png">Open the full runtime contact sheet</a></p><h2 id="rotation">Actual runtime rotation</h2><p>Representative Sparrow, narrow, broad, heavy, Viking, shield and carrier ships. The capture includes menu/hangar rotation and side/rear/underside inspection. The interactive model remains freely rotatable in-game.</p><video src="runtime-rotation.webm" controls preload="metadata"></video><h2>Offline construction inspection</h2><p>Blender inspection renders, clearly separate from the game captures. This section follows the selected ship above.</p><div class="pair"><figure><img id="clay" loading="lazy"><figcaption>Offline neutral clay</figcaption></figure><figure><img id="under" loading="lazy"><figcaption>Offline underside clay</figcaption></figure></div><h2 id="technical">Measured costs and validation</h2><div id="metrics"></div><p><a href="contracts.json">Fleet contract checks</a> · <a href="performance.json">Isolated runtime measurements</a> · <a href="packaged-assets.json">Packaged asset hashes</a> · <a href="steam-delivery.json">Steam test-build receipt</a></p><p class="note">The fleet uses maintained painted surfaces, restrained service staining, clear framed canopies and recessed propulsion. Cabin interiors remain suggestions; glass has no optical refraction. Several designs share manufacturing and propulsion families. The visual result is yours to judge; asset counts and validators are not a quality score.</p><p>No gameplay, combat sprites, progression, saves, achievements or leaderboard behavior changed. No public/default Steam promotion is included.</p></main><script>let pose='hero';const ship=document.querySelector('#ship');function show(){const id=ship.value;document.querySelector('#main').src=document.querySelector('#mainLink').href='evidence/'+id+'-menu-'+pose+'.png';document.querySelector('#hangar').src=document.querySelector('#hangarLink').href='evidence/'+id+'-hangar-hero.png';document.querySelector('#caption').textContent='Actual main menu - '+ship.selectedOptions[0].text+' - '+pose;document.querySelector('#clay').src='ships/'+id+'/renders/clay.png';document.querySelector('#under').src='ships/'+id+'/renders/underside-clay.png'}ship.onchange=show;document.querySelectorAll('[data-pose]').forEach(b=>b.onclick=()=>{pose=b.dataset.pose;show()});document.querySelectorAll('[data-ship]').forEach(b=>b.onclick=()=>{ship.value=b.dataset.ship;pose='hero';show();document.querySelector('#selected').scrollIntoView({behavior:'smooth'})});show();Promise.all([fetch('fleet.json').then(r=>r.json()),fetch('performance.json').then(r=>r.json())]).then(([fleet,p])=>{const a=p.rows.map(r=>r.frameMs.mean),q=p.rows.map(r=>r.frameMs.p95),sizes=fleet.map(r=>r.bytes/1048576),tris=fleet.map(r=>r.triangles);document.querySelector('#metrics').innerHTML='<p>'+p.gpu+'</p><table><tr><th>Showcase asset sizes</th><td>'+Math.min(...sizes).toFixed(1)+'–'+Math.max(...sizes).toFixed(1)+' MiB</td></tr><tr><th>Triangles per model</th><td>'+Math.min(...tris).toLocaleString()+'–'+Math.max(...tris).toLocaleString()+'</td></tr><tr><th>Mean frame time across scenes</th><td>'+Math.min(...a).toFixed(2)+'–'+Math.max(...a).toFixed(2)+' ms</td></tr><tr><th>Worst scene p95</th><td>'+Math.max(...q).toFixed(2)+' ms</td></tr><tr><th>Runtime errors / warnings</th><td>'+p.errors.length+' / '+p.warnings.length+'</td></tr></table><p>'+p.measurementNote+'</p>'}).catch(()=>{});</script></html>`;
await fs.writeFile(`${root}/review.html`,html);console.log('FLEET_REVIEW_READY',rows.length);
